Go to terminal, in root dir:
Enter npm install to install modules from package.json
Enter npm start to start the application. This runs on port 3000. Enter localhost:3000 in web browser url address bar.
